name: ci
on: [push, pull_request, tag, label]
jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest # TODO
    steps:
      - uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - uses: actions/checkout@v3
      - run: go run ci/main.go gha